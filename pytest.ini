[pytest]
# Put the repository root first on sys.path so `aerlink` and `tests` resolve here
# rather than to anything of the same name installed in site-packages.
pythonpath = .
testpaths = tests
addopts = -q
